import React, { Component } from 'react';

export default class CreateUser extends Component {
  render() {
    return (
      <div>
        create form here, possibly a dialog though
      </div>
    );
  }
}
